# Set folder for caching data

Set folder for caching data

## Usage

``` r
ll_set_folder(path = NULL)
```

## Arguments

- path:

  A path to a location. If the folder does not exist, it will be
  created.

## Value

The path to the caching folder, if previously set.
