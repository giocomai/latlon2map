# Find file names. Mostly used internally

Find file names. Mostly used internally

## Usage

``` r
ll_find_file(geo, level, resolution, year, name = "abl", file_type = "rds")
```

## Arguments

- name:

  Name of specific dataset being downloaded. Defaults to abl, i.e.
  administrative boundary line

- file_type:
